// src/controllers/dataController.ts

import { getStaticData, getDynamicData } from "systeminformation";

export const staticDataController = () => {};

export const dynamicDataController = () => {};
